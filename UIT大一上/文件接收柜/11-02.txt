30.数组指针:
	定义一个一维数组,定义一个int类型指针p,通过移动指针遍历并输出该数组
	(不允许使用p+i形式输出)


31.行指针:
	定义一个2x3的数组num,定义一个能指向该数组的行指针p,
	使指针指向该数组的首地址.然后操作指针输出该数组的
	num[0][0],num[1][0],num[2][2].

	
32.排序:
	设计函数(功能将传入的数组从小到大排序)
	不允许改动已有代码,在规定范围内添加代码完成题目要求
	附:1.c
	
	


33.排序:
	输入后不能改变原数组里的信息将数组由大到小输出到屏幕上
	不允许改动已有代码,在规定范围内添加代码完成题目要求
	附:2.c